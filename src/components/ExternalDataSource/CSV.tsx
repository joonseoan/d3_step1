import { useRef, useEffect } from "react";
import d3 from "d3";

const CSV = () => {
  const ref = useRef<HTMLDivElement | null>(null);
  const h = 100;
  const w = 400;
  // let ds;

  useEffect(() => {
    // d3.csv()
  }, []);

  return <div />;
};

export default CSV;
